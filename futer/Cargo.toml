[package]
name = "futer"
version = "0.1.0"
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[features]
libcosti = []
libc = []

[dependencies]
futex_ffi = { path = "../futex_ffi", features = ["libc"] }
